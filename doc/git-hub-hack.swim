git-hub-hack
============

How to hack on `git-hub`.

= Synopsis

  git hub fork ingydotnet/git-hub
  git hub clone git-hub
  cd git-hub
  make dev-install
  man git-hub-hacker
  # … write some tests …
  # … hack hack hack …
  make test
  make dev-test
  git hub pr-new


= Welcome Young Hacker

If you are reading this, hopefully you want to contribute to the `git-hub`
command. Welcome!

The `git-hub` command does a lot of things
