#!bash

main() {
  local root="$BASH_SOURCE"
  root="$(dirname $root)"
  root="$(dirname $root)"
  root="$(dirname $root)"
  export PATH="$root/lib:$PATH"
  export MANPATH="$root/man:$MANPATH"
#   if [ "$(type -f __gitcomp 2> /dev/null)" != function ]; then
#     if [ -e /etc/bash_completion.d/git ]; then
#       source /etc/bash_completion.d/git
#     else
#       source "$root/lib/git-hub.d/git-completion"
#     fi
#     source "$root/lib/git-hub.d/completion"
#   fi
}

main "$@"
